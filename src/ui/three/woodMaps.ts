/**
 * @deprecated Use boardMaterialMaps — kept as thin re-export for any residual imports.
 */
export {
  preloadBoardMaterialMaps as preloadWoodMaps,
  useBoardMaterialMaps as useWoodMaps,
  type BoardMaterialMaps as WoodMaps,
} from './boardMaterialMaps';
