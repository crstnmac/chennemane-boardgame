import { modelUrl } from './quality';

/** Cache-busted model URLs — single source for game + home. */
export const BOARD_URL = modelUrl('/models/board.glb') + '?v=hq7';
export const SEED_URL = modelUrl('/models/seed.glb') + '?v=hq7';
export const COCONUT_SHELL_URL = modelUrl('/models/coconut_shell.glb') + '?v=1';
